# 내맘대로 중요하다고 생각하는 거 자바스크립트 복습하기

## 1. 자바스크립트의 "객체(Object)"란?

자바스크립트에서 중괄호 `{ }`를 쓰면, "이름표(key)"와 "값(value)"을 짝지어 담는 상자를 만들 수 있다. 이걸 "객체"라고 부른다.

```js
{ 'text-danger': isWarning }
```

이것도 똑같은 객체다. 그냥 이름표와 값이 하나씩만 있는 것이다:

- 이름표(key): `'text-danger'` → "어떤 클래스를"
- 값(value): `isWarning` (지금 true 또는 false) → "어떤 조건일 때"

객체는 "조건" 그 자체가 아니라, "어떤 클래스를", "어떤 조건일 때" 붙일지를 짝지어서 담아두는 그릇이다.

객체는 "이름표와 조건을 하나씩 짝지어 여러 개 담을 수 있는 상자"이고, 그 안에 든 각각의 조건(true/false)이 "이 클래스를 붙일지 말지"를 결정한다. 즉, 이름표(key값)에는 클래스의 이름이 있는 것이다.

## 2. const가 뭔가? (자바스크립트 기본 문법)

변수를 만드는 법 세 가지(var, let, const) 중 하나로, 그냥 변수(상자)를 만드는 자바스크립트 문법이다.

- `const 이름 = 값` → 뜻: "이름이라는 상자를 만들고, 그 안에 값을 넣어라"
- `const myAge = 25` → 뜻: myAge라는 이름의 상자를 만들고, 그 안에 25라는 숫자를 넣은 것이다.

const의 특징: 한 번 상자를 만들면, 그 상자 자체를 다른 상자로 통째로 바꿀 순 없다. (근데 상자 안의 내용물을 바꾸는 건 상황에 따라 가능하다)

## 3. ref()는 왜 필요한가?

먼저 ref() 없이, 그냥 평범한 변수를 상상해보자.

```js
let count = 0
count = 5
```

이렇게 값을 바꾸면, 화면에 있는 숫자도 자동으로 같이 바뀔까, 안 바뀔까?

**화면이 자동으로 안 바뀐다!**

왜 화면이 자동으로 안 바뀔까?

Vue는 "이 변수, 내가 감시해야 하는 애야?"를 미리 알아야만 화면을 다시 그려준다. 그냥 `let count = 0` 이렇게만 쓰면, Vue 입장에서는 그게 그냥 평범한 자바스크립트 변수인지, Vue가 감시해야 할 특별한 변수인지 구별할 방법이 없다.

비유: 반 학생이 100명인데, 선생님(Vue)이 "누구 성적이 바뀌었는지" 다 일일이 확인하고 다닐 순 없다. 그래서 "나 성적 바뀌면 꼭 확인해주세요!"라고 미리 등록한 학생만 선생님이 챙겨준다.

그래서 ref()가 하는 일:

```js
const count = ref(0)
```

이 코드가 하는 일을 정확히 말하면:

- 0이라는 값을 그냥 평범한 상자에 넣는 게 아니라
- ref()라는 특수 포장지로 감싸서
- "이건 Vue가 감시해야 하는 특별한 변수야!"라고 Vue한테 등록하는 것이다

비유: `ref(0)`은 "0이라는 값을, 감시 카메라가 달린 특수 상자에 넣는다"는 뜻이다. 이 상자 안의 내용물이 바뀌면, 감시 카메라(Vue)가 바로 알아채고 화면을 다시 그려준다. 이때, 데이터의 형태와 상관 없이 모두 반응형 데이터로 만들어준다! (reactive()는 안 그렇다. 그래서 현업에서는 ref를 쓰는 경향이 있다고 한다.)

## 4. .value가 뭔가?

ref()로 감싸면, 그 값은 특수 상자 안에 들어가게 된다고 했다. 그럼 그 상자 안에 든 진짜 내용물을 꺼내려면 어떻게 할까?

```js
const count = ref(0)
console.log(count)         // 이렇게 하면 상자 자체가 나온다 (내용물이 바로 안 보임)
console.log(count.value)   // 이렇게 해야 진짜 내용물(0)이 나온다
```

`.value`는 "이 특수 상자를 열어서, 안에 든 진짜 값을 꺼내라"는 뜻이다.

값을 바꿀 때도 똑같다.

```js
count.value = 5   // 상자를 열어서, 안의 내용물을 5로 바꿔라
```

이렇게 `.value`를 통해서 값을 바꾸면, Vue가 그 변화를 딱 알아채고 화면을 자동으로 다시 그려준다.

근데 왜 `<template>` 안에서는 `.value`를 안 썼을까?

이건 Vue가 배려해주는 부분이다. `<script>` 태그 안(자바스크립트 코드)에서는 `.value`를 꼭 붙여야 하지만, `<template>` 태그 안(화면 부분)에서는 Vue가 자동으로 `.value`를 붙여서 처리해준다. 그래서 편하게 `count`라고만 써도 된다.

정리하면,

- `<script>` 안 (자바스크립트 코드)에서 값을 읽거나 바꿀 때: `count.value` (.value 필수)
- `<template>` 안 (화면 부분)에서 값을 읽거나 바꿀 때: `count` (.value 생략 가능, Vue가 알아서 처리)

### 위에 나온 개념들 코드 해석으로 최종 정리!!!

```js
const profileImg = ref('guest.png')
```

- `const` → profileImg라는 변수 상자를 만들어라 (자바스크립트 기본 문법)
- `ref('guest.png')` → 'guest.png'라는 값을, Vue가 감시할 수 있는 특수 상자로 포장해라
- 이 변수를 나중에 `profileImg.value = '내사진.png'`처럼 바꾸면 → Vue가 감지하고 화면을 자동으로 다시 그림
- `<img :src="profileImg" />` → `:src`가 이 변수를 계속 지켜보고 있다가, 값이 바뀌면 그 즉시 이미지도 자동으로 바꿔줌

## 5. Vue 파일(.vue)의 일반적인 순서

왜 이 순서가 자연스러울까? "재료 준비 → 그 재료로 요리 → 플레이팅" 순서다:

1. `<script>` — 먼저 어떤 데이터(ref로 만든 변수)와 함수가 있는지 준비한다
2. `<template>` — 그 준비한 데이터를 가지고 화면에 어떻게 보여줄지 그린다
3. `<style>` — 마지막으로 그 화면에 디자인을 입힌다

## 6. 왜 자바스크립트보다 Vue가 더 편할까? (Virtual DOM)

자바스크립트가 HTML 내용을 바꿀 수 있다는 건데, Vue는 바로 이 일을 훨씬 편하게 해주는 도구다. 순수 자바스크립트로는 "이 요소를 찾아서 → 내용을 바꿔라"라고 일일이 명령해야 하는데, Vue는 "데이터가 바뀌면 화면이 알아서 따라 바뀌게" 만들어준다.

자바스크립트는 뭐 하나가 바뀌면, 그것만 고치는 게 아니라 전체를 다 일일이 보고 고침. 예를 들어 고장난 부분이 화장실 세면대인데, 온 집안 점검을 다 하는 느낌. 뷰는 그 반대.

Vue의 해결책이 Virtual DOM, 즉 메모리 안에 만든 가짜 설계도다. 흐름은 이렇다.

데이터가 바뀌면 진짜 DOM을 바로 건드리지 않고, 가짜 설계도를 먼저 고친다. 가짜는 그냥 자바스크립트 객체라서 고치는 데 비용이 거의 안 든다. 그다음 "고치기 전 가짜"와 "고친 후 가짜"를 비교해서 실제로 달라진 부분만 찾아낸다. 그리고 그 최소한의 부분만 진짜 DOM에 반영한다.

개발자 입장에서 중요한 건 "이걸 내가 신경 쓸 필요가 없다"는 것이다. MVVM에서 "데이터만 바꾸면 Vue가 화면을 그려준다"고 했는데, 그 "그려준다"를 Vue가 최대한 싸게 하는 방법이 Virtual DOM이다. 원리만 알아두고, 코드에서는 그냥 데이터만 바꾸면 된다.

## 7. CSR vs SSR

CSR(Client-Side Rendering)은 밀키트 배달이다. 서버가 보내주는 건 거의 빈 HTML 껍데기와 큰 자바스크립트 파일 하나. 브라우저가 그 자바스크립트를 받아서 실행하면서 화면을 직접 조립한다. Vue의 기본 방식이 이거다.

SSR(Server-Side Rendering)은 완성된 음식 배달이다. 서버가 데이터까지 다 넣어서 완성된 HTML을 만들어 보내주고, 브라우저는 받자마자 그냥 보여주면 된다. Vue로 이걸 하려면 Nuxt.js라는 도구를 얹어야 한다.

장단점은 이 비유에서 그대로 나온다.

- **첫 화면 속도**는 SSR이 빠르다. 완성품이 오니까 받자마자 보이고, 밀키트는 조립 시간(JS 다운로드와 실행)이 걸린다. 그래서 CSR은 접속 직후 잠깐 흰 화면이 보이는 일이 있다.
- **페이지 이동 속도**는 반대로 CSR이 빠르다. 밀키트는 이미 재료가 브라우저에 다 있어서 다른 페이지로 갈 때 서버에 안 물어보고 바로 바꿔 그린다. SSR은 페이지마다 서버가 새로 만들어 보내야 해서 매번 조금씩 기다린다.
- **SEO(검색 노출)**는 SSR이 유리하다. 검색엔진 봇이 페이지를 훑을 때 CSR은 빈 껍데기만 보여서 "내용 없는 페이지"로 오해할 수 있고, SSR은 완성된 글이 있으니 제대로 읽어간다.

정리하면, 로그인 후 쓰는 관리 화면이나 앱처럼 한 번 들어와서 오래 쓰는 서비스는 CSR(기본 Vue)이 맞고, 블로그나 쇼핑몰처럼 검색으로 유입되고 첫 화면이 빨라야 하는 서비스는 SSR(Nuxt)을 고려하는 식이다.

## 8. SPA vs MPA

SPA라는 말은 CSR 방식으로 만든 사이트를 부르는 이름이다. 페이지 이동 때 서버에서 새 HTML을 안 받고 한 페이지 안에서 내용만 갈아끼우니까 Single Page Application(이것의 반대가 MPA)이다.

SPA(Vue 방식)는 처음 딱 한 번만 index.html과 자바스크립트를 받는다. 그 뒤로는 서버에서 HTML을 다시 받는 일이 없다. 게시글 목록을 보고 싶으면 서버에 "글 목록 데이터만 줘"라고 요청하고, 서버는 HTML이 아니라 JSON만 보내준다. 그러면 브라우저의 자바스크립트가 그 데이터를 받아서 화면의 필요한 부분만 갈아끼운다.

그래서 서버가 두 개로 나뉜 것도 보인다. 정적 파일 서버는 처음에 index.html과 JS를 한 번 주는 역할, 백엔드 API 서버는 이후 계속 JSON 데이터만 주는 역할. fetch + Promise + JSON.parse가 이 API 요청 부분에서 쓰이는 것이다.

한 문장으로 하면, MPA는 "페이지마다 서버에서 HTML을 새로 받는다", SPA는 "HTML은 처음 한 번, 이후엔 데이터만 받아서 자바스크립트가 화면을 고친다"이다.

장점 세 가지는 전부 "HTML은 한 번, 이후엔 데이터만"에서 나온다.

- **즉각적인 UX** — 페이지 이동 때 서버에 안 가니까 화면이 깜빡임 없이 바로 바뀐다는 것. 카카오톡 PC 버전에서 채팅방 옮길 때 새로고침이 없는 것처럼.
- **네트워크 효율** — CSS, JS를 매번 다시 받지 않고 처음 받은 걸 계속 쓰고, 이후엔 작은 JSON만 오가니까 통신량이 줄어든다는 것.
- **프론트/백엔드 분리** — 화면은 Vue 개발자가, 데이터는 백엔드 개발자가 각자 맡으면 되니까 팀이 나눠서 일하기 좋다는 뜻이다.

단점 두 가지:

- **초기 로딩** — 처음에 자바스크립트를 통째로 받아 실행해야 해서 첫 접속이 느릴 수 있다는 것.
- **SEO 취약** — 검색 봇이 빈 index.html만 보고 "내용 없는 페이지"로 판단할 수 있다는 것. 그래서 검색 노출이 중요한 서비스는 Nuxt(SSR)를 쓴다는 흐름이다.

## 9. 프로젝트 파일 4개가 연결되는 흐름

이번엔 4개 파일이 각자 뭘 하는지보다, "이 파일들이 서로 어떻게 연결돼서 하나의 화면을 만들어내는지" 흐름으로 이해하는 게 핵심이다.

전체 그림을 요리 준비 과정에 비유하면:

- `package.json` → 장보기 목록 (어떤 재료/도구가 필요한지 적어둔 것)
- `index.html` → 빈 접시 (브라우저가 맨 처음 여는 단 하나의 그릇)
- `main.js` → 요리 시작 버튼 (재료를 가져다가 조리를 시작하는 코드)
- `App.vue` → 완성된 첫 요리 (실제로 접시에 담기는 뼈대 화면)

**1단계: package.json** — "이 프로젝트를 실행하려면 vue, vue-router가 필요하다"는 걸 적어둔 명세서. `npm install`을 실행하면 이 목록을 보고 node_modules 폴더에 실제로 그 재료들을 다운로드해준다. 여기선 아직 아무것도 실행되지 않는다.

**2단계: index.html** — 브라우저가 접속하면 가장 먼저 이 파일을 읽는다. 근데 이 안엔 딱 두 가지만 있다:

- `<div id="app"></div>` → 텅 빈 그릇. 지금은 아무것도 안 담겨있다.
- `<script src="/src/main.js">` → "이 그릇을 채울 요리사(main.js)를 불러와서 실행시켜라"

즉, index.html은 "화면"이 아니라 "화면을 채워줄 코드를 실행시키는 스위치"다.

**3단계: main.js** — 여기서 실제 작업이 시작된다:

- `createApp(App)` → App.vue를 가지고 Vue 앱을 하나 만든다 (아직 화면엔 안 보임)
- `app.use(router)` → 라우터 기능을 장착한다
- `app.mount('#app')` → 드디어! index.html에 있던 그 텅 빈 그릇에 App.vue로 만든 화면을 실제로 끼워 넣는다

**4단계: App.vue** — 실제로 화면에 담기는 첫 화면(뼈대). `<RouterView />`가 핵심이다! "주소창이 뭐냐에 따라, 여기 자리에 다른 페이지(컴포넌트)를 끼워 넣어라"는 빈칸이다.

## 10. 디렉티브란?

"명령, 지시"라는 뜻의 영어 단어다. Vue에서는 HTML 태그에 붙여서 Vue한테 특별한 지시를 내리는 특수 속성을 말한다.

일반 HTML 속성 (그냥 정보를 적어두는 것):

```html
<p class="text-danger">안녕하세요</p>
```

→ 브라우저한테 "이 태그는 text-danger라는 이름표를 달고 있어"라고 그냥 알려줄 뿐이다.

Vue 디렉티브 (Vue한테 동작을 지시하는 것):

```html
<p v-if="isWarning">경고입니다</p>
```

→ Vue한테 "isWarning이 true일 때만 이 태그 자체를 화면에 그려라!"라고 명령을 내리는 것이다.

`v-`로 시작하는 속성은 전부 디렉티브다.

| 디렉티브 | 하는 일 |
|---|---|
| v-bind (줄여서 `:`) | 속성을 자바스크립트 값과 연결해라 |
| v-html | 문자열을 진짜 HTML로 해석해서 넣어라 |
| v-on (줄여서 `@`) | 이벤트(클릭 등) 발생 시 코드를 실행해라 |
| v-if | 조건에 따라 태그를 화면에 그릴지 말지 결정해라 |
| v-for | 배열/목록을 반복해서 태그를 여러 개 찍어내라 |

한 줄 요약: 디렉티브는 `v-`로 시작하는 Vue만의 특수 문법이고, HTML 태그한테 "이렇게 동작해라"는 명령을 내리는 도구다.

## 11. v-bind / v-model / v-on

**v-bind** — "HTML 속성을 자바스크립트 값과 동적으로 연결한다"

```html
<img :src="profileImg" />
```

축약형: `v-bind:` → `:`. class 말고 src, disabled 등 어떤 속성에도 쓸 수 있다. 동적으로 바꾼다는 건, 코드를 직접 고치지 않아도 변수 값이 바뀌기만 하면 화면(속성)이 자동으로 그 값을 따라간다는 뜻. v-bind 없이 그냥 `src="cat.jpg"`라고 쓰면 고정값(평생 cat.jpg)이고, `:src="변수"`를 쓰면 변수에 따라 자동으로 바뀌는 값.

**v-model** — "입력창(input)의 값과 변수를 양방향으로 연결한다"

```html
<input v-model="userName" />
```

사용자가 타이핑하면 → 변수가 자동으로 바뀜. 코드에서 변수를 바꾸면 → 입력창도 자동으로 바뀜. v-bind가 "한쪽 방향(변수→화면)"만 되는 거라면, v-model은 "양쪽 다"(화면↔변수) 되는 게 핵심 차이다.

**v-on** — "클릭이나 키 입력 같은 이벤트가 일어나면, 지정한 코드를 실행한다"

```html
<button @click="isWarning = !isWarning">토글</button>
```

축약형: `v-on:` → `@`.

셋의 관계를 한 줄로 정리하면:

- v-bind(`:`) → 읽기 전용 연결 (변수 값을 화면 속성에 반영)
- v-on(`@`) → 동작 연결 (이벤트가 일어나면 코드 실행)
- v-model → 사실 v-bind + v-on을 한 번에 합쳐놓은 것 (내부적으로 "값을 보여주기"랑 "입력되면 값 바꾸기"를 동시에 처리)

## 12. 새 파일은 어느 폴더에 만들까?

프론트 웹페이지를 만들 때, 직접 내가 새 파일을 만들 때 "이걸 어디에 쓸 건지"를 스스로 정해서 원하는 폴더에 만드는 거다!!

- 재사용할 작은 부품(버튼, 카드, 아이콘 등)을 만들고 싶다 → `components` 폴더 (또는 그 안의 icons 같은 하위 폴더)
- URL 하나에 대응하는 완전한 페이지를 만들고 싶다 → `views` 폴더
- 여러 화면이 같이 쓰는 전역 상태(스토어)를 만들고 싶다 → `stores` 폴더 (과제 5에서 추가됨, 아래 13번부터)
- 외부 서버와 통신하는 코드를 만들고 싶다 → `api` 폴더 / 바뀌지 않는 고정 데이터를 두고 싶다 → `constants` 폴더 (과제 6에서 추가됨, 아래 18번부터)

## 13. 스토어(Pinia)는 왜 필요한가?

지금까지 상태(ref)는 전부 "어떤 컴포넌트 안"에 살았다. 근데 이러면 두 가지 문제가 생긴다.

**문제 1: 상태가 컴포넌트와 운명을 같이한다.** 즐겨찾기 목록을 홈 뷰 안에 ref로 뒀더니, /favorites 페이지 갔다가 돌아오는 순간 홈 컴포넌트가 새로 만들어지면서 즐겨찾기가 싹 리셋됐다. 컴포넌트는 페이지 이동 때마다 부서졌다 다시 지어지는 건물이라, 그 안의 상태(가구)도 같이 사라지는 것이다.

**문제 2: 멀리 있는 컴포넌트끼리 상태를 나눠 쓰기 힘들다.** 온도 단위(섭씨/화씨)는 헤더의 버튼이 바꾸는데, 그 값을 읽어야 하는 건 홈의 카드, 상세 페이지, 즐겨찾기 카드다. props로 전달하려면 App.vue → 헤더/각 뷰 → 카드까지 온 가족이 택배를 릴레이로 전달해야 한다 (이걸 props drilling이라고 부른다).

비유: 컴포넌트 안의 ref가 "내 방 서랍"이라면, 스토어는 "거실 냉장고"다. 어느 방(컴포넌트)에 있든 거실로 나오면 같은 냉장고를 열 수 있고, 방을 리모델링해도(컴포넌트가 다시 만들어져도) 냉장고 내용물은 그대로다.

사실 이 문제를 절반쯤 해결한 걸 이미 만들어봤다 — `useFavorites.js`의 모듈 스코프 ref(미니 전역 상태)다. Pinia는 그 수법을 공식 도구로 만든 것이라고 보면 된다. 대신 Pinia는 이름 붙은 스토어 단위로 정리되고, devtools에서 상태 변화를 추적할 수 있고, state/getters/actions라는 정해진 구조가 있어서 규모가 커져도 안 흐트러진다.

## 14. defineStore와 state / getters / actions

과제 5에서 만든 `stores/configStore.js`의 뼈대:

```js
export const useConfigStore = defineStore('config', {
  state: () => ({ unit: 'celsius' }),
  getters: {
    unitSymbol: (state) => (state.unit === 'celsius' ? '℃' : '℉'),
  },
  actions: {
    toggleUnit() {
      this.unit = this.unit === 'celsius' ? 'fahrenheit' : 'celsius'
    },
  },
})
```

세 칸의 역할을 컴포넌트에서 배운 개념과 짝지으면 바로 이해된다:

| 스토어 | 컴포넌트에서의 짝 | 역할 |
|---|---|---|
| state | ref | 원본 데이터를 담는 상자 |
| getters | computed | state를 가공해서 보여주는 파생 값 (state가 바뀌면 자동 재계산) |
| actions | 일반 함수(메서드) | state를 바꾸는 공식 창구 |

주의할 점 몇 개:

- `state`는 객체가 아니라 **객체를 돌려주는 함수** `() => ({...})`로 쓴다.
- `defineStore('config', ...)`의 첫 인자 `'config'`는 devtools에서 이 스토어를 구분하는 고유 이름표다.
- actions 안에서는 `this.unit`처럼 `this`로 state에 바로 접근한다. `.value`도 필요 없다.
- 내보내는 이름은 `useConfigStore`처럼 use로 시작하는 게 관례다 (composable이랑 같은 규칙).

등록도 라우터랑 똑같은 패턴이다. main.js에서 `use(createPinia())`를 해줘야 컴포넌트 어디서든 `useConfigStore()`로 꺼내 쓸 수 있다. (라우터 때 `use(router)` 했던 것과 같은 "기능 장착" 단계)

## 15. storeToRefs — 구조분해하면 반응성이 끊긴다?

컴포넌트에서 스토어를 쓸 때 제일 잘 틀리는 부분이다.

```js
const configStore = useConfigStore()

// ❌ 이렇게 꺼내면 반응성이 끊긴다!
const { unit, unitSymbol } = configStore

// ✅ state/getter는 storeToRefs로 꺼내야 반응형이 유지된다
const { unit, unitSymbol } = storeToRefs(configStore)

// ✅ action은 그냥 함수라서 바로 꺼내도 된다
const { toggleUnit } = configStore
```

왜 그럴까? 구조분해는 "상자에서 내용물을 꺼내서 복사"하는 것이다. ref 3번에서 배운 비유로 말하면, 감시 카메라가 달린 특수 상자에서 내용물만 꺼내버리니까 카메라(반응성 연결)가 끊기는 것. `storeToRefs`는 내용물 대신 "특수 상자 그 자체"를 하나씩 나눠주는 도구라서 연결이 유지된다.

action은 애초에 감시 대상(데이터)이 아니라 그냥 함수라서 복사해도 아무 문제 없다. 그래서 UnitToggler.vue에서 state/getter는 `storeToRefs`로, `toggleUnit`은 그냥 구조분해로 꺼냈다.

참고로 구조분해 안 하고 `configStore.unitSymbol`처럼 스토어 통째로 들고 점(.)으로 접근하면 반응성 걱정이 아예 없다. WeatherCard와 상세 뷰에서는 이 방식을 썼다.

## 16. 과제 5에서 스토어를 실제로 쓴 그림

전체 흐름을 한 줄로 요약하면: **버튼(액션 호출) → 스토어 state 변경 → 그 state를 보는 모든 화면이 동시에 갱신**.

1. 헤더의 UnitToggler에서 [단위변경] 클릭 → `toggleUnit()` 액션 실행 → `unit`이 `'celsius'` ↔ `'fahrenheit'`로 토글
2. `unitSymbol` getter(℃/℉)를 보고 있던 곳들 — 헤더 문구, 홈 카드, 평균 기온, 상세 페이지 — 이 전부 즉시 다시 그려짐
3. 기온 숫자는 원본(섭씨)을 건드리지 않고, 각 컴포넌트의 `displayTemp` computed가 스토어의 `convert` getter로 변환해서 표시만 바꿈

여기서 배운 설계 포인트 두 가지:

- **원본 데이터는 안 바꾼다.** 화씨로 바꿀 때 weatherList의 temp를 직접 고치는 게 아니라, 보여줄 때만 computed로 변환한다. 원본을 건드리면 다시 섭씨로 돌아올 때 반올림 오차가 쌓인다.
- **공식(로직)은 한 곳에만 둔다.** 화씨 변환 공식을 카드/상세/홈에 각각 적으면 중복인데, 스토어의 `convert` getter에 한 번만 적고 다들 호출하게 했다. `convert: (state) => (celsius) => {...}`처럼 getter가 함수를 돌려주게 하면 인자를 받는 getter도 만들 수 있다 (함수형 getter).

## 17. composable vs 스토어 — 언제 뭘 쓰나?

이 프로젝트에는 전역 상태가 두 가지 방식으로 공존한다. `useFavorites`(composable의 모듈 스코프 ref)와 `configStore`(Pinia). 정리하면:

- **composable** — 로직 재사용이 주 목적. 가볍고 설치가 필요 없다. 즐겨찾기처럼 "상태 하나 + 토글 함수 하나" 수준이면 충분하다.
- **Pinia 스토어** — 앱 전역 설정처럼 여러 화면이 같이 보는 상태가 주 목적. state/getters/actions 구조 강제 + devtools 추적이 장점이라, 상태가 늘어나고 규모가 커질수록 유리하다.

둘 다 "컴포넌트 밖에 상태를 두면 컴포넌트가 죽어도 상태는 산다"는 같은 원리 위에 있다. 배우는 입장에서는 useFavorites로 원리를 손으로 만들어보고, configStore로 공식 도구를 써본 셈이라 비교가 됐다.

나중에 하나 더 추가한 `useDisplayTemp`는 셋 중 또 다른 용도다 — 상태를 갖지 않고 **로직(섭씨 → 현재 단위 문자열 조립)만 재사용**하는 composable. "변환 공식은 스토어 getter에, 표시 조립은 composable에, 상태는 스토어에"로 역할이 나뉜다. 과제 5 슬라이드가 "메인/상세 중복 → Composable로 해결 가능(범위 제외)"이라고 힌트만 준 부분을 직접 구현해본 것.

## 18. axios — 서버에 데이터를 "주문"하는 도구

8번(SPA)에서 "이후엔 서버에 데이터만 달라고 요청하고 JSON을 받는다"고 했는데, 그 "요청하는 코드"를 편하게 쓰게 해주는 라이브러리가 axios다. 브라우저 내장 fetch로도 되지만, axios는 JSON 변환을 자동으로 해주고(fetch는 `.json()`을 직접 호출해야 함), 아래 나오는 "인스턴스" 기능이 있어서 실무에서 많이 쓴다.

비유: API 호출은 식당 주문이다. 주문서(요청)를 보내면 → 주방(서버)이 만드는 동안 기다렸다가 → 음식(JSON 응답)이 온다. 여기서 중요한 건 **음식이 바로 안 온다**는 것. 이게 19번(비동기)의 주제다.

과제 6에서 쓴 핵심 기능은 **인스턴스**다:

```js
const owm = axios.create({
  baseURL: 'https://api.openweathermap.org/data/2.5',
  params: { appid: API키, units: 'metric', lang: 'kr' },
})
// 이제 owm.get('/weather', { params: { lat, lon } }) 처럼 짧게 호출
```

매 호출마다 반복될 주소 앞부분과 공통 파라미터(키/단위/언어)를 "단골 주문서 양식"으로 한 번만 등록해두는 것이다. 이걸 `api/weatherApi.js` 한 파일에 두니까, 컴포넌트에는 URL도 키도 안 보이고 `fetchCurrentWeather(lat, lon)` 같은 함수 이름만 남는다.

## 19. async / await와 Promise.all — 기다림을 다루는 문법

서버 응답은 즉시 오지 않는다 (수십 ms~수 초). 자바스크립트는 그동안 멈춰서 기다리지 않고 다음 일을 계속하는데(비동기), 이때 "나중에 도착할 값"을 감싼 상자가 **Promise**다. 주문하면 바로 받는 건 음식이 아니라 진동벨이고, 진동벨이 울리면(응답 도착) 그때 음식을 받는 구조.

**async/await**는 이 진동벨 기다리기를 평범한 코드처럼 쓰게 해주는 문법이다:

```js
const load = async () => {          // await를 쓰려면 함수에 async를 붙여야 함
  const data = await fetchCurrentWeather(lat, lon)  // 응답이 올 때까지 이 줄에서 기다림
  weatherList.value = ...           // 도착한 다음에 실행됨
}
```

**Promise.all**은 여러 주문을 동시에 넣고 전부 도착할 때까지 한 번만 기다리는 도구다. 과제 6에서 두 번 썼다:

- 홈: 5개 도시 날씨 — 순서대로 `await`를 5번 하면 (한 도시 0.3초 기준) 1.5초 걸리는데, `Promise.all`로 동시에 던지면 제일 느린 것 하나만큼(0.3초)만 걸린다.
- 상세: 현재날씨 + 예보 + 미세먼지 3종 동시 호출.

```js
const [cur, fc, ap] = await Promise.all([현재날씨(), 예보(), 미세먼지()])
```

**실패 처리는 try/catch**로 한다. await 중에 에러가 나면(네트워크 끊김, 키 오류) catch로 떨어진다. 과제 6의 포인트: 상세 페이지에서 날씨 3종과 위키 소개의 try를 **분리**했다 — 위키가 실패해도 날씨 화면은 살아야 하니까. "필수 데이터"와 "부가 데이터"는 실패했을 때의 운명이 달라야 한다.

## 20. 비동기 UI의 3분기 — 로딩 / 실패 / 성공

mock 시절엔 데이터가 항상 그 자리에 있었지만, 실제 API는 "아직 안 옴"과 "못 받아옴"이라는 상태가 생긴다. 그래서 화면도 최소 세 갈래가 필요하다:

```html
<p v-if="isLoading">불러오는 중...</p>
<div v-else-if="loadError">에러 문구 + [다시 시도] 버튼</div>
<div v-else>진짜 데이터 화면</div>
```

같이 따라온 함정: **데이터가 빈 상태로 실행되는 코드**. 응답이 오기 전 `weatherList`는 빈 배열이라, 평균 기온(0으로 나누기)이나 `reduce`(빈 배열 + 초기값 없음 = 에러)가 터진다. 그래서 computed마다 `length` 가드를 먼저 세웠다. 비동기 데이터로 바꾸는 순간 "첫 렌더링 때 이 값이 비어 있어도 안 터지나?"를 전부 다시 점검해야 한다는 걸 배웠다.

하나 더 — API 응답은 우리 화면 사정을 모른다. OWM은 날씨를 `Clear`/`Clouds`/`Rain`처럼 주는데 우리 화면은 '맑음'/'구름'/'비' 기준으로 돌아간다. 그래서 응답을 받자마자 우리 형식으로 바꾸는 **번역 계층**(`toStatus` 매핑)을 api 폴더에 뒀다. 덕분에 화면 코드는 API가 뭘 주든 몰라도 된다.

## 21. .env와 API 키 — "숨긴 것"이 아니라 "커밋 안 한 것"

API 키는 코드에 직접 쓰면 안 되니까 `.env` 파일에 두고 `.gitignore`로 저장소에 안 올라가게 한다. Vite에서는 변수 이름이 **`VITE_`로 시작해야만** `import.meta.env.VITE_WEATHER_API_KEY`로 읽힌다 (접두사 없는 변수는 실수로 노출되지 않게 Vite가 아예 안 넘겨줌).

근데 여기서 착각하면 안 되는 것: **프론트엔드의 .env 키는 비밀이 아니다.** 빌드하면 그 값이 JS 파일 안에 문자열로 박혀서, 배포하면 브라우저 개발자도구로 누구나 볼 수 있다. `.gitignore`는 "저장소에 안 올리는 것"이지 "사용자에게 숨기는 것"이 아니다. 결제 키처럼 진짜 비밀인 키는 백엔드 서버가 대신 호출해주는 구조(프록시)여야 한다. 이번 OpenWeatherMap 무료 키는 노출돼도 피해가 제한적이라 프론트에서 직접 호출하는 구조로 충분하다.

**.env vs .env.example** — `.env`를 숨기기만 하면 저장소를 받은 다른 사람은 "무슨 환경변수가 필요한지"조차 알 수 없다. 그래서 실제 값 대신 자리표시자만 적은 `.env.example`(양식)을 만들어 이것만 저장소에 올린다. 받는 사람은 복사해서 `.env`로 이름 바꾸고 자기 키를 채우면 됨. `.gitignore`의 `!.env.example` 줄이 "다른 .env는 다 무시하되 이 양식만은 예외로 올려라"는 뜻(`!`가 예외 표시). 비유하면 `.env`는 작성 완료된 서류, `.env.example`은 빈칸만 있는 신청서 양식이다.

## 22. 디버깅 사례 — "오타"처럼 보이던 것의 진짜 원인 찾기

제주 상세 페이지의 위키백과 소개 문구가 "제주특벨ㅈ · 치도"처럼 깨져 보였다. 딱 보면 오타 같지만, 외부 API가 준 데이터라 내가 낸 오타일 리는 없다. 그럼 어디가 문제일까?

**1단계: 원인 후보를 층으로 나눈다.** 화면에 글자가 나오기까지는 세 층을 거친다 — ① 데이터(위키백과가 준 원문이 이상한가?) ② 코드(내가 받아서 가공하다 망가뜨렸나?) ③ 렌더링(브라우저/폰트가 제대로 못 그리나?). 버그처럼 보이는 걸 만나면 "어느 층 문제인지"부터 갈라야 헛다리를 안 짚는다.

**2단계: 원본 데이터를 눈이 아니라 문자 코드로 확인한다.** API 응답을 문자 단위로 유니코드 코드를 찍어봤더니, 문제 구간은 `ᄌ`(U+110C, 초성 지읒) + `ᆞ`(U+119E, 중성 아래아)였다. 오타가 아니라 **제주어 표기 "제주특벨ᄌᆞ치도"**였던 것. 아래아(ㆍ)는 현대 한글에선 사라졌지만 제주어에는 살아 있는 모음이라, 위키백과가 옛한글 자모로 정확하게 적어둔 거다. 여기서 배운 것: 텍스트가 이상해 보이면 "보이는 모양"을 믿지 말고 실제 코드값을 까봐야 한다. 눈에는 같아 보여도 다른 문자인 경우(옛한글, 전각/반각, 유사 문자)가 있다.

**3단계: 그럼 왜 깨져 보였나.** 완성형 한글(예: '제' U+C81C)은 한 글자가 코드 하나지만, 옛한글은 초성+중성 낱자를 조합해서 표시해야 한다. 이 조합은 폰트가 지원해야 하는데 일반 폰트는 못 해서, 브라우저가 낱자를 따로따로 그리며 "ㅈ ·"처럼 보인 것. 결론: **데이터 정상, 내 코드 정상, 폰트(렌더링) 문제.**

**4단계: 해결은 "어디 층에서 고칠지" 고르는 문제.** 렌더링 층에서 고치려면 옛한글 지원 폰트를 넣어야 하는데, 사용자 컴퓨터에 폰트가 없으면 여전히 깨지고 웹폰트는 병기 한 줄 때문에 수 MB를 받는 배보다 배꼽 구조. 그래서 한 층 위(데이터 가공)에서 해결했다 — 어차피 여행 소개 카드에 `(한자, 제주어, 영어)` 병기는 군더더기라, 제목 바로 뒤 괄호 덩어리를 잘라내는 `stripLeadingParen`을 `wikiApi.js`에 추가. 문제가 원천 차단되고 문장도 "제주특별자치도는 대한민국의…"로 시작해 오히려 읽기 좋아졌다.

**구현에서 배운 것 두 가지:**

- **괄호 제거는 정규식보다 개수 세기.** `(서울特別市, 영어: Seoul (Special City))`처럼 괄호 안에 또 괄호가 있으면 단순 정규식 `\(.*?\)`는 안쪽 닫는 괄호에서 멈춰 반쪽만 지운다. 여는/닫는 괄호 개수를 세다가(depth) 0이 되는 지점까지가 진짜 한 덩어리다.
- **확신 없으면 안 건드리는 게 방어적 처리.** 괄호가 제목 바로 뒤가 아니거나(문장 중간 괄호), 짝이 안 맞으면 자르지 않고 원문을 그대로 돌려준다. 데이터 가공 함수는 "잘못 자르는 것"이 "안 자르는 것"보다 훨씬 나쁘다.

그리고 이 로직을 컴포넌트가 아니라 api 폴더(응답을 우리 형식으로 다듬는 번역 계층)에 둔 것도 20번에서 배운 원칙 그대로다 — 화면 코드는 위키백과가 옛한글을 주든 말든 몰라도 된다.

## 23. UI 라이브러리(Element Plus) — 남이 만든 부품 조립하기

지금까지 버튼/카드/배지를 전부 CSS로 직접 만들었는데, 과제 7에서 외부 UI 라이브러리로 갈아탔다. UI 라이브러리는 "잘 만든 컴포넌트 모음집"이다 — `<el-button>`, `<el-card>`처럼 태그만 쓰면 디자인·호버·접근성까지 완성된 부품이 나온다.

**왜 쓰나?** 직접 만든 검색창을 `el-input`으로 바꿨더니 지우기(X) 버튼이, 위키 사진을 `el-image`로 바꿨더니 클릭 확대 보기가, 로딩 문구를 `el-skeleton`으로 바꿨더니 회색 뼈대 애니메이션이 **공짜로** 생겼다. 이런 걸 다 직접 만들면 각각 반나절짜리다. 대신 라이브러리의 디자인 언어를 따라야 하는 제약이 생기는데, 이게 다음 주제로 이어진다.

**배운 것 1: 테마는 CSS 변수로 뚫는다.** Element Plus 기본색은 파랑인데 우리 앱은 인디고다. 라이브러리 CSS를 뜯어고치는 게 아니라, 라이브러리가 색을 전부 CSS 변수(`--el-color-primary` 등)로 참조하도록 만들어져 있어서 `:root`에서 변수만 덮으면 전체 컴포넌트가 한 번에 우리 톤이 된다. 1번(객체)에서 배운 "이름표-값" 구조가 CSS에도 있는 셈이다.

**배운 것 2: 전역 등록 vs 필요한 것만.** main.js에서 `use(ElementPlus)` 한 줄이면 모든 컴포넌트를 어디서든 쓸 수 있어 편한데, 안 쓰는 컴포넌트까지 번들에 들어가 빌드가 100kB → 1MB로 커졌다. 실무에선 쓰는 것만 자동으로 골라 담는 방식(unplugin)을 쓴다. "편함 ↔ 성능"의 전형적인 트레이드오프.

**배운 것 3: 관심사 분리의 보상.** 이번 과제에서 stores/composables/api 폴더는 한 줄도 안 고쳤다. 과제 3~6 동안 "로직은 밖으로, 화면은 템플릿에"를 지켜온 덕분에 UI를 통째로 갈아끼우는 데 템플릿 수정만으로 끝난 것. 반대로 로직이 템플릿에 엉켜 있었다면 UI 교체가 로직 재작성이 됐을 거다.

**배운 것 4: 전부 바꾸는 게 능사가 아니다.** 날씨 카드의 틀과 별(☆) 버튼은 직접 만든 걸 유지했다 — 선택 시 테두리 강조, 호버 애니메이션 같은 우리만의 동작이 있어서 라이브러리 부품으로 바꾸면 오히려 기능이 줄어든다. 라이브러리는 "맞는 부품만 골라 쓰는 것"이지 신앙이 아니다.

**배운 것 5: 반응형 레이아웃도 라이브러리 부품으로.** UI 라이브러리엔 버튼·카드 같은 "보이는 부품"만 있는 게 아니라 레이아웃 시스템도 있다. `el-row`/`el-col`은 한 줄을 24칸으로 나누는 그리드인데, `<el-col :xs="24" :md="12">`라고 쓰면 "모바일(xs)에선 24칸(= 한 줄 다 차지, 1열), 데스크톱(md 이상)에선 12칸(= 반 줄, 2열)"이 된다. media query를 직접 쓰지 않아도 화면 폭에 따라 자동으로 접혔다 펴진다. 왜 하필 24칸이냐면 24는 2·3·4·6·8·12로 다 나눠떨어져서 어떤 열 개수든 만들기 좋아서다.

## 24. API 요청의 5요소 — Postman으로 직접 확인해보기

수업에서 "어떤 API든 호출하려면 알아야 하는 5가지"를 Postman으로 실습했다. Postman은 코드 없이 버튼으로 API 요청을 테스트하는 도구다 — axios가 하는 일을 손으로 해보는 셈이라, 18~19번에서 코드로 배운 걸 눈으로 재확인할 수 있었다.

5요소를 실제 URL에 대입하면:

```
https://api.openweathermap.org/data/2.5/weather?lat=37.4058453&lon=127.0998294&appid={키}&lang=kr
└──────────────── ① URL ────────────────┘ └──────────── ③ 보내는 데이터(쿼리 파라미터) ────────────┘
```

- **① 요청할 URL** — 물음표 앞부분. "어느 가게에 주문하는가"
- **② 요청 방식** — 데이터 조회니까 GET (주소창에 쳐도 되는 방식). 서버에 뭔가 만들어달라고 보낼 땐 POST
- **③ 보내야 할 데이터** — 물음표 뒤 `키=값&키=값` 쿼리 파라미터. Postman의 Params 탭이 이걸 표로 분해해서 보여줌
- **④ API KEY** — `appid` 파라미터. 일부러 한 글자 지우고 보내면 401이 나서, 키가 "인증"이라는 걸 체감할 수 있다
- **⑤ 받는 데이터의 양식** — Send 누르면 나오는 JSON. 이 구조를 알아야 코드에서 `data.main.temp`처럼 꺼내 쓸 수 있다

**직접 확인한 것들:**

- `"temp": 305.86` — 처음 보고 뭔가 했는데 **켈빈 단위**였다. `units` 파라미터를 안 보내면 OWM 기본값이 켈빈이다 (305.86 − 273.15 ≈ 32.7℃). `units=metric`을 추가하면 섭씨로 온다. "③에서 보내는 파라미터가 ⑤의 응답 양식을 바꾼다"는 걸 눈으로 확인한 순간. 우리 앱이 axios 인스턴스에 `units: 'metric'`을 박아둔 이유이기도 하다.
- `"name": "Seongnam-si"` — 좌표(판교)만 보냈는데 서버가 지명을 알아서 찾아준다.
- 같은 키로 `data/2.5/weather`는 200, `onecall` 계열(2.5/3.0/4.0)은 401이었다. One Call은 별도 구독 상품이라 신규 무료 키로는 접근이 막혀 있어서다. "같은 키가 어떤 URL에선 되고 어떤 URL에선 안 된다 = 키 문제가 아니라 권한(플랜) 문제"라는 판별법도 익혔다.

**API 키 취급 주의** — 키를 채팅, 과제 제출 문서, 깃허브처럼 남에게 보이는 곳에 붙여넣지 않기. `.env`에 두고 이름(`VITE_WEATHER_API_KEY`)으로만 언급하는 습관 (21번 참고).

## 25. localStorage — 새로고침에도 살아남는 저장소

"내 여행" 기능을 만들면서 새 문제를 만났다. 여행 계획을 Pinia 스토어에 넣었는데, **새로고침하면 사라진다.** 13번에서 "스토어는 컴포넌트가 죽어도 산다"고 했지만, 그건 페이지 이동(컴포넌트 교체) 얘기고 — 새로고침은 자바스크립트 전체가 처음부터 다시 실행되는 거라 스토어(메모리)도 초기화된다.

그래서 브라우저가 제공하는 **localStorage**를 썼다. 메모리가 아니라 디스크에 남는 키-값 저장소라 새로고침·브라우저 종료에도 살아남는다.

```js
localStorage.setItem('weather-trip-plan', JSON.stringify({ cityId, date })) // 저장
JSON.parse(localStorage.getItem('weather-trip-plan') ?? 'null')            // 읽기
localStorage.removeItem('weather-trip-plan')                               // 삭제
```

포인트 몇 개:

- localStorage는 **문자열만** 저장한다. 객체는 `JSON.stringify`로 문자열로 만들어 넣고, 꺼낼 땐 `JSON.parse`로 되돌린다 (8번 SPA에서 나온 그 JSON이 여기서도 쓰임).
- **역할 분담**: 스토어는 "지금 화면이 쓰는 메모리", localStorage는 "껐다 켜도 남는 창고". 둘을 잇는 방법은 간단하다 — 스토어 action에서 값을 바꿀 때마다 localStorage에도 쓰고, 스토어 초기값을 localStorage에서 읽어온다 (tripStore.js가 이 구조).
- 날짜는 Date 객체 말고 `'YYYY-MM-DD'` **문자열로 통일**해서 저장했다. Date 객체는 JSON으로 갔다 오면 문자열이 돼버려서 처음부터 문자열로 다루는 게 덜 헷갈린다.
- 즐겨찾기(useFavorites)는 localStorage를 안 쓰니 새로고침하면 사라진다. 필요해지면 같은 수법으로 붙이면 된다.

**API 제약을 기능 설계로 감싸기** — 이 기능의 진짜 배움은 따로 있다. 무료 예보는 5일까지라 "2주 뒤 제주 날씨"는 못 보여준다. 그런데 "여행을 등록해두면 5일 전부터 자동으로 보여준다"로 설계를 바꾸니, 같은 제약이 사용자에겐 "앱이 알아서 챙겨준다"는 경험이 됐다. 기술 한계를 없애는 게 아니라 흐름을 바꿔서 안 느껴지게 만드는 것도 설계다.
